/**
 * TikTok profile and featured videos for the website.
 * TikTok doesn't provide a public API to pull your feed, so add video URLs here.
 * Get URLs by opening a video on TikTok → Share → Copy link.
 */

export const tiktokProfile = {
  username: 'thepiratemaxx',
  /** Optional: add your TikTok profile image URL (e.g. from CDN or public host) */
  profileImageUrl: '', // e.g. '/images/tiktok-avatar.png' or a CDN URL
  displayName: 'Pirate Maxx',
};

/** Full TikTok video URLs to show on the site. Add new links here to feature more videos. */
export const tiktokVideoUrls: string[] = [
  // Paste full URLs from TikTok: open a video → Share → Copy link
  // Example: 'https://www.tiktok.com/@thepiratemaxx/video/7123456789012345678',
];
